#!/bin/sh{{! Templated With Moustache. We need to redefine the template engine delimiters, because they look like BASH delimiters. }}{{=<% %>=}}

#get local scripts for this benchmark
export PATH=$PATH:$PBS_O_WORKDIR/../../scripts

<% cmdline %>

